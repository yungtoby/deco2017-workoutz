# Simple python script to start the application without hassle:
import os

# Initialize node package manager to run the
# build and serve the init files to the webserver.
os.system("npm run build\nnpm run serve")